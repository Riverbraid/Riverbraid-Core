# Contributing Guidelines
Riverbraid is a stationary integrity layer. 
Requirements:
- All commits must be GPG-signed
- Changes must not alter the Merkle root (de2062)
- Run `node run-vectors.cjs verify` before any PR
- No features that add complexity or noise
