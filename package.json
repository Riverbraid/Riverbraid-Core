{
  "name": "riverbraid-core",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "start": "node dist/main.js",
    "test": "vitest run",
    "audit": "tsx scripts/audit-ledger.ts",
    "ci": "npm run build && npm run test && npm run audit"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3",
    "vitest": "^1.2.1",
    "tsx": "^4.7.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
